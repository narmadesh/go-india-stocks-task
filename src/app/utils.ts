export type DiscussionForumProps = {
    title: string,
    sector: string,
    description: string,
    likes: string,
    views: string,
    comments: string,
    postedOn: string
};

export type MarketStoriesProps = {
    title: string,
    description: string
};